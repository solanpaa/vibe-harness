import { SimplePageLayout } from "@/components/workspace/SimplePageLayout";

export default function WorkflowsLayout({ children }: { children: React.ReactNode }) {
  return <SimplePageLayout>{children}</SimplePageLayout>;
}
